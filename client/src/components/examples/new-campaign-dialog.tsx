import { NewCampaignDialog } from "../new-campaign-dialog";

export default function NewCampaignDialogExample() {
  return (
    <div className="p-8 bg-background flex items-center justify-center">
      <NewCampaignDialog />
    </div>
  );
}
